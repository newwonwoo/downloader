name: Downloader worker integration

on:
  push:
    branches: [main, "migrate/**", "fix/**"]
    paths:
      - "downloader-worker/**"
      - ".github/workflows/worker-integration.yml"
  pull_request:
    paths:
      - "downloader-worker/**"
      - ".github/workflows/worker-integration.yml"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  worker:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.13'
      - name: Install worker dependencies
        run: python -m pip install -r downloader-worker/requirements.txt httpx
      - name: Install matching ffmpeg binaries
        shell: bash
        run: |
          set -euo pipefail
          cd downloader-worker
          mkdir -p .bin
          ffmpeg_path="$(python -c 'import imageio_ffmpeg; print(imageio_ffmpeg.get_ffmpeg_exe())')"
          ffprobe_path="$(python -c 'import imageio_ffmpeg; print(imageio_ffmpeg.get_ffprobe_exe())')"
          ln -sf "$ffmpeg_path" .bin/ffmpeg
          ln -sf "$ffprobe_path" .bin/ffprobe
          .bin/ffmpeg -version | head -n 2
      - name: Run worker tests
        run: PATH="$PWD/downloader-worker/.bin:$PATH" python -m unittest discover -s downloader-worker -p 'test_*.py' -v
      - name: Start worker locally
        shell: bash
        run: |
          set -euo pipefail
          (cd downloader-worker && PATH="$PWD/.bin:$PATH" uvicorn app:app --host 127.0.0.1 --port 18080 >/tmp/worker.log 2>&1) &
          echo $! >/tmp/worker.pid
          for attempt in $(seq 1 30); do
            if curl -fsS --max-time 2 http://127.0.0.1:18080/health >/tmp/health.json; then
              cat /tmp/health.json
              break
            fi
            sleep 1
          done
          test -s /tmp/health.json
          python - <<'PY'
          import json
          data=json.load(open('/tmp/health.json'))
          assert data['ok'] is True
          assert data['mode']=='native-mobile-stream-v5-files'
          PY
          status=$(curl -sS -o /tmp/jobs.json -w '%{http_code}' -H 'content-type: application/json' -d '{}' http://127.0.0.1:18080/jobs)
          test "$status" = "422"
      - name: Diagnostics
        if: always()
        run: cat /tmp/worker.log || true
